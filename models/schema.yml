version: 2

sources:
  - name: raw
    schema: gz_raw_data
    tables:
      - name: sales
        identifier: gz_raw_sales
      - name: product
        identifier: raw_gz_product
      - name: ship
        identifier: raw_gz_ship
